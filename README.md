# 💼 FinPocket

**FinPocket** — локальное PWA-приложение для учёта личных финансов, долгов и коммунальных показаний. Приложение работает
**офлайн** благодаря IndexedDB/Dexie.js, поддерживает установку как **Progressive Web App** и синхронизируется с любимыми облаками
только по запросу.

> 💡 FinPocket = *финансы в твоём кармане* — простота, автономность, безопасность.

Онлайн-демо: [FinPocket на GitHub Pages](https://outofops.github.io/FinPocket).

---

## 🚀 Быстрый старт

```bash
npm install
npm start
```

После запуска откройте [http://localhost:4200](http://localhost:4200) — приложение автоматически перезагрузится при сохранении файлов.

### Сборка и PWA

```bash
npm run build
```

Готовый билд лежит в `dist/FinPocket/browser`. Service Worker подключается автоматически (в продакшене), манифест и иконки находятся в
`public/`.

---

## 🧰 Стек

- [Angular](https://angular.dev/) (версия 20.3) с маршрутизацией и SCSS
- [Angular Material](https://material.angular.io/) с кастомной тёмной темой в духе Monobank (`#9C27B0` и `#FF9800` в роли акцентов)
- [Dexie.js](https://dexie.org/) — тонкая обёртка над IndexedDB для офлайн-хранилища
- Service Worker и manifest для полноценного PWA-опыта

---

## 🧱 Архитектура модулей

```
src/app
├── core/      # Сервисы платформы, Dexie, Http и пр.
├── shared/    # Общие Angular Material модули и утилиты
├── finance/   # Учёт операций, бюджетов и счетов
├── debts/     # Управление взаиморасчётами и напоминаниями
├── meters/    # Учёт показаний коммунальных счётчиков
├── stats/     # Аналитика, графики и отчёты
├── sync/      # Синхронизация, резервные копии и облачные профили
└── settings/  # Настройки приложения, темы и валюты
```

Каждый функциональный модуль подключается лениво через маршрутизатор и использует общие UI-компоненты из `shared/`.

---

## 🎨 Дизайн и UX

- Тёмная тема по умолчанию, вдохновлённая Monobank
- Акценты: `#9C27B0` (primary) и `#FF9800` (accent)
- Навигация на базе `mat-sidenav` + `mat-toolbar`
- Глобальные компоненты-«карточки» для ключевых разделов
- Типографика: **Poppins** для заголовков, **Inter** для текста

---

## 🗄️ Локальное хранилище

`FinPocketDatabase` (Dexie.js) описывает базовые таблицы:

- `transactions` — операции и счета
- `debts` — займы, статусы и дедлайны
- `meters` — показания коммунальных счётчиков
- `syncQueue` — очередь изменений для облака

База доступна как сервис с `providedIn: 'root'` и готова для расширения миграциями.

---

## 🤝 Вклад

1. Форк или ветка
2. `npm install`
3. Вносите изменения, запускайте `npm test`
4. Pull Request приветствуется! :)

